# HTTP Servers

Implementation of various HTTP servers with different connection types:
  - Non-persistent
  - Persistent
  - Pipelined

We compared the performances between each server as detailed in `server-performance-report.pdf`
